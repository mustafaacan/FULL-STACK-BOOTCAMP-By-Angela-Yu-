REACT

 the most powerful advantage of the React dividing the whole structure and managing is more easy. 
 For instance, assume that there are lots of products with details such picture or price. Wıth creating one sample can be used within
 the page as component. with using props feature for component and using a loop, many products can be rendered at once.

 Divide option also can provide more advanced rendering opportunity because in any change, only responsive
 component will be updated and many times, to see the updates, we do not refresh the page

 ** CAUTION: EJS is using for express side to manage HTML rendering directly from server side. 
 If we are using React, the response data from server, can be send as JSON 
 
 res.json(....)


JSX: Creating HTML-Based structures by using JS.


DUMMY EXAMPLE

React Frontend
     |
     | HTTP Request
     | fetch / axios
     ↓
Express Backend
     |
     | JSON Response
     ↓
React


EXPRESS SIDE

const express = require("express");

const app = express();

app.get("/api/products", (req, res) => {
  const products = [
    {
      id: 1,
      name: "MacBook Pro",
      price: 80000
    },
    {
      id: 2,
      name: "iPhone",
      price: 50000
    }
  ];

  res.json(products);
});

app.listen(3000, () => {
  console.log("Server running on port 3000");
});


REACT SIDE

import { useEffect, useState } from "react";

function Products() {
  const [products, setProducts] = useState([]);

  useEffect(() => {
    fetch("http://localhost:3000/api/products")
      .then(response => response.json())
      .then(data => {
        setProducts(data);
      });
  }, []);

  return (
    <div>
      {products.map(product => (
        <div key={product.id}>
          {product.name} - {product.price} TL
        </div>
      ))}
    </div>
  );
}

export default Products;